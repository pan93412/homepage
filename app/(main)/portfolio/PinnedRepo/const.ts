/**
 * When will the followers cache in components
 * be invalidated, in seconds?
 *
 * In other words, how long should we fetch the data again?
 */
export const REVALIDATE_SECONDS = 3 * 60 * 60; // 3hr
